# Curated Data

This directory holds intentionally curated source lists that are safe to keep in git.

- `reddit-sources.csv` is the final reviewed Reddit source audit used for seed/import transparency.

Generated discovery, raw scrape, and intermediate audit CSVs are ignored at the repository root.
